_base_: exp_baseline_stable.yaml

output_dir: outputs/exp_acq_only_stable

acq:
  enabled: true
  lambda_acq: 0.02
  small_area_thr: 1024
  topk_unmatched: 30
  delta: 0.03
  sigma: 0.05
  min_score: 0.5
  apply_last_n_layers: 2

train:
  debug_dir: outputs/exp_acq_only_stable/debug

