_base_: exp_baseline_stable.yaml

output_dir: outputs/exp_rsnds_only_stable

rsnds:
  enabled: true
  beta: 1.0
  gamma_base: 1.0
  gamma_min: 0.5
  gamma_max: 1.0
  reliability: cls_conf

train:
  debug_dir: outputs/exp_rsnds_only_stable/debug

