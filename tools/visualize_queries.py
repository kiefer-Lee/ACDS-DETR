"""Placeholder entry for query/reference-point visualization.

Run inference with `export_sampling_locations` or inspect `outputs["reference_points"]`
from the model, then overlay normalized points on the original image.
"""

